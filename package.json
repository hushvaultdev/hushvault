{
  "name": "hushvault",
  "version": "0.0.1",
  "private": true,
  "description": "Cloudflare-native secrets manager monorepo",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "type-check": "turbo type-check",
    "test": "turbo test",
    "playwright:install": "playwright install --with-deps",
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.47.0",
    "turbo": "^2.3.0",
    "typescript": "^5.7.0"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  },
  "packageManager": "pnpm@10.33.0",
  "pnpm": {
    "overrides": {
      "shell-quote": ">=1.8.4",
      "esbuild": ">=0.28.1",
      "ws": ">=8.21.0"
    }
  }
}
